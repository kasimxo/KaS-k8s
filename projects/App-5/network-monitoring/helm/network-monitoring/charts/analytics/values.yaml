replicaCount: 1

image:
  repository: analytics
  tag: v2
  pullPolicy: Never

redis:
  host: network-monitoring-redis
  port: 6379

service:
  type: ClusterIP
  port: 8000
  targetPort: 8000
  protocol: TCP